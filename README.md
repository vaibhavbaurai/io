# Initialize a new repository
git init

# Clone an existing repository
git clone https://github.com/username/project.git

# Configure user information
git config --global user.name "Vaibhav Baurai"
git config --global user.email "vaibhavbaurai128@gmail.com"

# Check configuration
git config --list
